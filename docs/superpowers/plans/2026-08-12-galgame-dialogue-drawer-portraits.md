# Galgame 沉浸式对话抽屉与立绘升级实施计划

1. 为默认隐藏、悬浮按钮、抽屉开关、焦点恢复和移动端语义编写失败测试。
2. 在 `TavernDialogue` 中加入抽屉状态、焦点管理与 Escape 行为，重组现有记录/选项/输入结构但不改变数据流。
3. 在全局样式中将 Galgame 场景改为主画布，新增桌面右侧抽屉和手机底部抽屉，并补齐 reduced-motion。
4. 运行对话定向测试与生产构建，在桌面和 375px 视口进行浏览器验收。
5. 逐一使用 Image 2 参考现有角色立绘生成成熟性感版本，去除纯色背景并验证 Alpha；替换项目资产。
6. 运行全量测试、构建、资产检查与独立代码审查。
7. 提交并推送 `main`，等待 Vercel 部署后复验线上对话页。
